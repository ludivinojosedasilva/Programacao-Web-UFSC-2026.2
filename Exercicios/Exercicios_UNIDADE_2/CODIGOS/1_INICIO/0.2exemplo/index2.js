// Variaveis, const, var)
let nome = "Node.js";
const versao = 20;

// Declaração de funções

function saudacoes(usuario) {
  return `Ola, ${usuario}!`; // String no formato  ES6
}

// Arrow function (ES6+)
const soma = (a, b) => a + b;

console.log(saudacoes("Maria"));
console.log(soma(10, 20));
