// Variaveis, const, var)
let nome = "Node.js";
const versao = 20;

// Declaração de funções

function saudacoes(usuario) {
  b = "Ana";
  return "Ola " + usuario;
}

function soma(a, b) {
  return a + b + outra;
}

console.log(saudacoes("Maria"));

for (let a = 0; a < 10; a++) {
  console.log(a);
}

outra = 200;

console.log(soma(10, 20));
